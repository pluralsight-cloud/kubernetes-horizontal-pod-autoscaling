apiVersion: v1
kind: Service
metadata:
  name: globomantics
  labels:
    app: globomantics
spec:
  type: ClusterIP
  selector:
    app: globomantics
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
